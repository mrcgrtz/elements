language: node_js
node_js:
- node

branches:
  only:
  - master
  - /^greenkeeper/.*$/

after_success:
- yarn run lint
- yarn run test --coverage --coverageReporters=text-lcov | ./node_modules/.bin/coveralls
